# WRF-Fire comparison for OpenBCEllipsoidBuffer

Same structure as `../../OpenBCRidgeCorner/wrf_comparison/`: the identical
physical setup (ellipsoid terrain, Open BCs on all four sides, 3 m/s @
34deg wind, top damping, horizontal advection order) run in WRF-Fire
instead of ERF, using WRF's file-based terrain input
(`fire_read_atm_ht`/`fire_read_fire_ht=.true.`, `input_ht`/`input_zsf`
generated by `gen_wrf_ellipsoid.py`, the same ellipsoid formula as ERF's
`../gen_ellipsoid_terrain.py`).

## Result

Two semi-major-axis values tested (`a=1700` and `a=2500`, see
`../README.md`'s clearance table). Both stay stable:

- `a=1700`: 3 -> ~5.1 m/s peak, settling to 4.4 m/s by t=360s.
- `a=2500` (narrower ~453 m x-clearance): 3 -> ~5.3 m/s peak, settling to
  4.6 m/s by t=360s -- essentially the same behavior despite the reduced
  buffer.

Matches ERF's stability on the same terrain in both cases. See
`../README.md` for the full tables and the conclusion this supports
(terrain-boundary proximity, not a general ERF Open-BC weakness, drove
the ridge case's runaway).

`input_ht`/`input_zsf`/`gen_wrf_ellipsoid.py` in this directory are the
current (`a=2500`) versions; see git history for the `a=1700` originals
if needed.

## Build and run

Same WRF-Fire checkout as `../../OpenBCRidgeCorner/wrf_comparison/`
(`WRF4_fire/WRF`, v4.6.1, commit `d66e442f`).
```
mkdir case_ellipsoid_buffer_34deg && cd case_ellipsoid_buffer_34deg
# copy namelist.input, namelist.fire, input_sounding, input_ht, input_zsf
# from this directory, plus the standard WRF static data files (see
# ../../OpenBCRidgeCorner/wrf_comparison/README.md for which ones)
ln -s ../ideal.exe ../wrf.exe .
./ideal.exe
./wrf.exe
```
`namelist.input` here has `run_minutes = 6` (360s), shortened from the
ridge case's 45 min since this test only needed to match ERF's 300s
comparison window.

## Figures

```
python3 plot_wind_terrain_wrf.py 0
python3 plot_wind_terrain_wrf.py 300
```
- `wind_terrain_wrf_t00000.png`, `_t00060.png`, `_t00300.png` -- `a=1700`,
  terrain + wind at the lowest model level, same conventions as
  `../../OpenBCRidgeCorner/wrf_comparison`'s figures (fixed quiver scale,
  0-300m terrain color range), minus the mixed-corner box overlays (not
  relevant here -- terrain never approaches a boundary).
- `wind_terrain_wrf_a2500_t00000.png`, `_t00060.png`, `_t00300.png` --
  `a=2500`, same conventions.
